export enum events {
  /** Emitted when a user registers */
  user_registered = 'user_registered',
  /** Emitted when a user requests a new password */
  user_password_reset_requested = 'user_password_reset_requested',
}
